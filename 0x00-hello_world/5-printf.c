#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: C program that prints exactly the line of text you want.
 *
 * Return: always 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
