#include <stdio.h>

/**
 * main - Entry Point
 *
 * Description: C program that prints all single digit numbers of base 10.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}
