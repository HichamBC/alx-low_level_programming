#include "main.h"

/**
 * mul - function that multiplies two integers.
 *
 * @a: first function parameter.
 * @b: seconde function parameter.
 *
 * Return: multiplication of two digits a and b.
 */

int mul(int a, int b)
{
	return (a * b);
}
